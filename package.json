{
  "scripts": {
    "dev": "concurrently \"shopify theme dev --store jordans-domaine-test\" \"npx @tailwindcss/cli -i ./assets/tailwind.input.css -o ./assets/tailwind.output.css --watch\""
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.0.12",
    "tailwindcss": "^4.0.12"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
